// variables.tfで定義した変数に実際の値を設定するファイル
// このファイルはGitにコミットしない場合もある (機密情報を含む場合)

// S3バケット名
// 注意: バケット名はAWS全体で一意である必要があるため、
// 適切な命名規則 (組織名-プロジェクト名-環境-日付など) を使用すること
bucket_name = "sekishiro-bucket-example-cloudfront_s3-20251213"

// 環境名
// dev: 開発環境, staging: ステージング環境, prod: 本番環境
environment = "dev"

// アップロードするファイルが格納されているフォルダのパス
// このフォルダ内の全ファイルが自動的にアップロードされる
upload_folder = "upload_file"

// S3内でのファイルのプレフィックス (オプション)
// 例: "uploaded/" とすると、S3内で uploaded/ フォルダに配置される
s3_prefix = "" // 空文字列の場合はバケットのルートにアップロード

// 画像アップロードを有効にするかどうか
enable_images_upload = true

// アクセスを許可するIPアドレスのリスト (CIDR形式)
// 例: ["1.2.3.4/32", "5.6.7.8/32"]
// 空のリストの場合、すべてのIPアドレスがブロックされます
allowed_ip_v4 = [""] # ← あなたのIPv4アドレス
allowed_ip_v6 = [""] # ← あなたのIPv6アドレス 
